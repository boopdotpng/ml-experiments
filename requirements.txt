# tinygrad is not on here: clone it into ./tiny (gitignored) and run
#   uv pip install -e ./tiny
# PyTorch is optional and hardware-specific. Install one of:
#   uv pip install -r requirements-torch-amd.txt
#   uv pip install -r requirements-torch-nvidia.txt
numpy
matplotlib
pillow
safetensors
transformers
